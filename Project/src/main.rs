use std::collections::HashMap;

mod stadium;

fn main() {
    let mut stadium = HashMap::new();

    zones.insert("Norte", create_zone("Norte"));
    zones.insert("Sur", create_zone("Sur"));
    zones.insert("Este", create_zone("Este"));
    zones.insert("Oeste", create_zone("Oeste"));

}

